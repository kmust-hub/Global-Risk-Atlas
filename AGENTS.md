# Project Workflow

- After completing any requested change in this repository, commit the relevant files and push to `origin/main` automatically.
- Do not wait for the user to explicitly ask for a GitHub sync.
- Run the relevant validation before committing.
- Keep commits scoped to the current request. Do not include unrelated user changes.
- If authentication, network, or a failing validation blocks the push, report the blocker clearly.
